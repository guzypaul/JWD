package by.guzypaul.branchingandloop.service.loop;

/**
 * @author Guzy Paul
 * Task32 (loop):check the string entered by the user for invalid characters.
 * First charachter can be only: letters and underscore.
 * Other characters can be: leters, digitals and underscore.
 */

public class StringChecker {

    public void checkStringFromUser(String userString) {
        boolean result = false;
        /*if(userString.charAt(0) == "letters" || userString.charAt(0) == "_"  ){
            result = true;
        }*/

        /*for (int i = 1; i < userString.length(); i++) {
            if(userString.charAt(i) == "letters/ digitals/, _ "){
                result =  true;
            }
        }
        return result;*/
    }
}